package builders

import main.kotlin.lexer.Token
import org.example.ast.ASTNode
import org.example.ast.IfNode
import rules.MatchedRule

class IfNodeBuilder : NodeBuilder {
    override fun buildNode(input: List<Token>) : ASTNode{

    }
}
