plugins {
    id 'org.jlleitschuh.gradle.ktlint' version '13.0.0'
}

ktlint {
    version = "0.50.0"
    ignoreFailures = false
    reporters {
        reporter "plain"
        reporter "checkstyle"
    }
}
